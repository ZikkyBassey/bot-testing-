# SolSignalX Bot

This bot integrates with DexScreener API, Solana RPC, and Twitter for trading signals.

## Features
- Fetch trading data from DexScreener.
- Interact with Solana blockchain.
- Post updates and signals on Twitter.

## Setup
1. Install dependencies: `pip install -r requirements.txt`
2. Configure your API keys and settings.